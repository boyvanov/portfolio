.scroll {
  font-size: 18px;
  font-weight: 600;

  &:after {
    content: "";
    @include inlineblock(middle);
    width: 20px;
    height: 30px;
    margin-left: 22px;
    background: svg-load('scroll.svg', fill=$text-color, width=100%, height=100%);
    @include bgcover;
  }
}